# Dataset Structure

`raw/`
Original OCR files from the LOC dataset.

`manifests/`
CSV files defining corpus slices used for indexing.

`processed/`
Parsed document representations used by the indexing pipeline.

`intermediate/`
Temporary files produced during indexing.